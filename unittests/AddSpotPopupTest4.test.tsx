//import {expect, describe} from '@jest/globals';
import AddSpotPopup from '../components/AddSpotPopup';

// Testing for this specific output would be difficult, so I asked the test to make sure the return value was not null
test('AddSpotPopup test',()=> expect(AddSpotPopup).not.toBeNull());