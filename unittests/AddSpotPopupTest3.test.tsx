//import {expect, describe} from '@jest/globals';
import AddSpotPopup from '../components/AddSpotPopup';

// Test to make sure AddSpotPopup is being created as a function so it can be properly called by other parts of the project

test('AddSpotPopup test',()=>expect(AddSpotPopup).toBeInstanceOf(Function))