//import {expect, describe} from '@jest/globals';
import AddSpotPopupProps from '../components/AddSpotPopup';

// Test to ensure the AddSpotPopupProps interface is properly defined and populated to avoid errors
test('AddSpotPopupProps test',()=> expect(AddSpotPopupProps).toBeDefined())